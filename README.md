
  # Homepage v2

  This is a code bundle for Homepage v2. The original project is available at https://www.figma.com/design/UfdjAsTBzyzRcC7jWveLfc/Homepage-v2.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  